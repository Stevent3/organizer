import { Screen } from '../components/Screen'

/** Planer-Tab: KI-Tagesplan + Essensplaner (im Aufbau) */
export function PlannerScreen() {
  return (
    <Screen title="Planer" subtitle="Tagesplan & Essen">
      <p className="mt-3 text-[14px] text-text-2">Kommt gleich.</p>
    </Screen>
  )
}
